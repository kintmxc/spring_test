<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.spring_test.mapper.RecommendationLogMapper">
    <insert id="insertLog">
        INSERT INTO recommendation_log (user_id, recommendation_type, recommendation_data, recommendation_time)
        VALUES (#{userId}, #{recommendationType}, #{recommendationData}, NOW())
    </insert>
</mapper>